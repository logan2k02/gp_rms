export const environment = {
  defaultAlertTimeout: 5000, // Default timeout for alerts in milliseconds
  backendUrl: 'http://localhost:3000', // Base URL for the backend API
  loggingEnabled: true,
};
